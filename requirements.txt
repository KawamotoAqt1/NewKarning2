# SVG+CSVからPhase1学習用JSONを生成するスクリプト群の依存関係

# XMLパース（標準ライブラリのxml.etree.ElementTreeを使用するため不要）
# 必要に応じてlxmlを使用する場合は以下をコメントアウト
# lxml>=4.9.0

# SVG path解析（正確なbbox計算のため推奨）
svgpathtools>=1.5.0

# 標準ライブラリのみで実装しているため、外部依存は最小限
# Python 3.7以上を推奨

